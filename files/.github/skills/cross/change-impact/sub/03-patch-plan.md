# Sub-03 — 补丁计划

## 目标

把影响矩阵拆成可执行任务，并给出复验顺序。

## 任务拆分规则

- P0/P1 必须生成任务
- 一个任务只改一个明确产物
- 每个任务必须有责任 Skill
- 有依赖时必须排在依赖之后
- 完成标准必须可验证

## 推荐执行顺序

默认顺序：

```
glossary → spec → DB → API → prototype → design-review
```

如变更来源是原型，仍需回写 spec/glossary 后再同步下游。

## 输出

- P0/P1/P2 补丁任务表
- 推荐执行顺序
- 最终复验入口
